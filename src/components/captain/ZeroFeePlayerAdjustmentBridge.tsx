"use client";

// Disabled: viewing the team payment ledger must never trigger a POST request,
// recalculate charges, or inject adjustment panels.
export default function ZeroFeePlayerAdjustmentBridge() {
  return null;
}
