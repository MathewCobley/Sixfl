// ========================================
// File: src/components/admin/AdminSidebarDesktopColumnsBridge.tsx
// ========================================

"use client";

/**
 * Retired compatibility shell.
 *
 * Former responsibilities now have explicit owners:
 * - Admin Kits navigation is rendered natively by AdminSidebar.
 * - Admin sidebar columns/layout are rendered natively by AdminSidebar.
 * - Queue View details links are owned by AdminQueueItemDetailsLinksBridge.
 *
 * Keep this inert shell until the replacement-contract inventory no longer
 * needs the historical bridge path. Do not add DOM mutation back here.
 */
export default function AdminSidebarDesktopColumnsBridge() {
  return null;
}
