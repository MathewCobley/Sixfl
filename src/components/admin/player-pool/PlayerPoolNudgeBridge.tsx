"use client";

// Retained temporarily because the admin layout still imports this component.
// The PlayerPool page renders individual and bulk reminder controls natively.
export default function PlayerPoolNudgeBridge() {
  return null;
}
