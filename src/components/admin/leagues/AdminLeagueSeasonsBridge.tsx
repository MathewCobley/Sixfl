"use client";

/** Retired: AdminLeagueSeasonsPanel is mounted natively in the league layout.
 * Keep this no-op export for existing admin layout imports; never inject or
 * rewrite React-owned season controls after render. */
export default function AdminLeagueSeasonsBridge() {
  return null;
}
