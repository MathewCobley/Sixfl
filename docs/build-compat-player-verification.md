# Player verification build compatibility

The player-email verification compatibility patch is deliberately idempotent and avoids exact multi-line anchors that can be changed by earlier build-time compatibility scripts.
