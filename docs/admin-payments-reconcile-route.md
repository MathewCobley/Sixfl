# Admin payment reconciliation route

Open `/admin/payments/reconcile` as an admin after deployment to reconcile existing paid squad/player match fees back to the related team charges.

The route is idempotent and can be run again if needed.
